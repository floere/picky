This folder contains tests that e.g. access the database etc. but can't be called full integration tests, since they only test a very specific part of Picky.

If you add a new backend on your own, this is the place to put the tests.